import 'package:flutter/material.dart';

class CompassPage extends StatelessWidget {
  const CompassPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Compass Page', style: TextStyle(fontSize: 24)),
    );
  }
}
